<!DOCTYPE html>
<html>
    <head>
        <title>ActionViewFormHelper</title>
        <meta name="viewport" content="width=device-width,initial-scale=1">
        <%= csrf_meta_tags %>
        <%= csp_meta_tag %>

        <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
        <%= javascript_importmap_tags %>
    </head>

    <body>
        <%= yield %>

        <div class="container">
            <% flash.each do |key, value| %>
                <%= value.html_safe %>
            <% end %>
        </div>
    </body>
</html>
