<p>
  <strong>Title:</strong>
  <%= @version.reify.title %>
</p>

<p>
  <strong>Body:</strong>
  <%= @version.reify.body %>
</p>
<%= button_to 'Revert to this version', revert_post_path(@version.item_id,@version.id), method: :post %><br/>
<%= link_to 'Back', versions_post_path(@version.item_id) %>