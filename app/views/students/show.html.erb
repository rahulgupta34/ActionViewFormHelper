<p style="color: green"><%= notice %></p>

<%= render @student %>

<div>
  <%= link_to "Edit this student", edit_student_path(@student) %> |
  <%= link_to "Back to students", students_path, data: { confirm: 'Are you sure?' } %>

  <%= button_to "Destroy this student", @student, method: :delete, remote: true %>
</div>

